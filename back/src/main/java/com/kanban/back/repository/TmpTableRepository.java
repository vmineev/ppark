package com.kanban.back.repository;

import com.kanban.back.entity.TmpTable;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TmpTableRepository extends JpaRepository<TmpTable, Integer> {
}
